Figure out how to extract item detail images to use on hover
Contact ddowiki admins (Discord or an admin talk page) about exempting `/api.php` from their AWS WAF bot challenge — standard scope-down; the API has its own throttling. If granted: (a) the pipeline gains automated parsed-data-vs-wikitext diffing for parser QA at full-database scale, (b) optionally restore an embedded wiki preview via `action=parse` + DOMPurify. Until then the Flag button ("Report mismatch") is the manual capture path.
Removed non-named items from items db
Parse everything from just the wiki rather than the ddo files
Crafting materials/systems view (own top-level view, not part of Resource view)

Inspiration: ddo-builds.com