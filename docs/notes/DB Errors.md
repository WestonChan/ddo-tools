Status legend: ✅ done · 🚧 in this phase · 📋 planned (future phase, see tag) · ❌ won't do · 🐛 bug

Running log of data-quality errors found in `public/data/ddo.db` while using the app. Each entry records the error type, evidence, and a reproduce query so the comprehensive ETL review (📋 Phase 4c) can work through them without re-diagnosing. Roadmap Phase 4c links here; scraper-side fixes belong in `scripts/`.

- 🐛 📋 Phase 4c — **Effect magnitudes stored in `effects.modifier` instead of `item_effects.value`; bonus type discarded.** Root cause confirmed (2026-07-24) against cached wikitext in `.wiki-cache/`: the wiki's enchantment-template grammar is `{{Effect|magnitude|bonus-type}}` — e.g. `Item:Legendary Large Shield of the Fallen Age` has `{{Incite|59|Insightful}}` and `{{Conditioning|15|Legendary}}`. But `parse_effect_template` (`scripts/src/ddo_data/dat_parser/effects.py`, the `len(params) >= 2` branch) assumes params[0] is a *textual* modifier, so the magnitude ("59") lands in the TEXT `modifier` column and the **actual bonus type ("Insightful"/"Legendary") is thrown away** — data loss that matters once stacking rules consume effects. Single-param templates parse correctly (`{{Incite|120}}` → `value=120`), which is why both shapes coexist for the same effect name; signed/percent single params (`{{Incite|+91}}`, `…|59%}}`) fail `isdigit()` and also land in modifier. Symptoms: numbers ("59", "20") render in the Enchantments type-chip column, and near-duplicate effect rows proliferate (`Incite` has ~40 rows differing only by magnitude). 149 effects rows affected (140 bare-numeric + 9 signed/percent) across 30 effect names, reaching 613 `item_effects` rows. Worst offenders: Diversion (20), Incite (20), Spell Resistance (18), Assassinate (8), DR (8), Armor-Piercing (7+4 as "ArmorPiercing"). Fix direction: teach `parse_effect_template` the `{{Effect|value|bonus-type}}` grammar — numeric-ish first param (int, `+N`, `N%`) → `value`, second param → modifier/bonus-type; keep the Clicky special case; then dedupe the effects table and re-key `item_effects`. Reproduce:
	- `SELECT name, modifier, COUNT(*) FROM effects WHERE modifier GLOB '[0-9]*' GROUP BY name, modifier;`
	- Parser-level: `parse_effect_template('{{Incite|59|Insightful}}')` → `{'effect': 'Incite', 'modifier': '59', 'value': None}` (should be `value=59`, modifier/bonus-type `'Insightful'`)
- 🐛 📋 Phase 4c — **Same name normalized two ways:** `Armor-Piercing` and `ArmorPiercing` coexist as separate effect names (7 and 4 numeric-modifier rows respectively). Effect-name normalization pass needed alongside the dedupe above.
- 🐛 📋 Phase 4c — **Four raids have zero `quest_loot` rows; two more have no `quests` row at all.** Found 2026-07-25 while wiring `quest_loot.loot_type`; expanded by the wiki reconciliation the same day. `The Vault of Night`, `The Shroud`, `The Lord of Blades`, and `The Codex and the Shroud` exist in `quests` but have no loot mappings, so none of their items are reachable via "Drops from" or the Raid filter — the loot_type backfill correctly tags nothing because there are no rows to tag. `Threats Old and New` and `Den of Vipers` (both on the wiki's `Raids` page) are missing from `quests` entirely. All are raids with well-known loot tables, so this is a scrape gap, not a game-data reality. Partial explanation from the reconciliation: `Category:Raid_loot` on the wiki hasn't been updated since 2015 and doesn't list any of them, so their loot — if categorized at all — sits under subcategories whose titles don't match these quest names (confirmed for The Shroud: its loot is under `Altar of Fecundity loot`, now handled via the raid list; the other three are unconfirmed — enumerate their loot subcategories in a browser when auditing). Worth auditing the whole subcategory-title → quest-name derivation. Reproduce:
	- `SELECT q.name, COUNT(ql.item_id) FROM quests q LEFT JOIN quest_loot ql ON ql.quest_id = q.id WHERE q.name IN ('The Vault of Night','The Shroud','The Lord of Blades','The Codex and the Shroud') GROUP BY q.id;` → all zero
	- `SELECT COUNT(*) FROM quests WHERE name IN ('Threats Old and New','Den of Vipers');` → 0
	- Contrast with a working raid: loot count for `'The Master Artificer'` → 43
- 🐛 📋 Phase 4c — **13 raid quests have `pack_id NULL`** — the rows auto-created by `insert_quest_loot` from loot-category discovery carry only a name (`Hunt or Be Hunted`, `Old Baba's Hut`, `Skeletons in the Closet`, `Project Nemesis`, `The Mark of Death`, `The Dryad and the Demigod`, `Altar of Fecundity`, `The Chronoscope (Legendary)`, and the five `Legendary *` re-releases). They're missing from `quest_seed_data.json`. The wiki's `Raids` page provides the authoritative pack per raid (e.g. `Skeletons in the Closet` → The Isle of Dread, `Old Baba's Hut` → Mists of Ravenloft, `Hunt or Be Hunted` → Hunter and Hunted); for bulk backfill beyond raids, `Quests_by_level_and_XP` is a DPL-generated table of every quest with pack + patron + level (see the "Finding quests and raids" section in [ddowiki-api.md](../ddowiki-api.md)). Fold them into the seed data. Reproduce: `SELECT q.name FROM quests q JOIN quest_loot ql ON ql.quest_id=q.id WHERE ql.loot_type='raid' AND q.pack_id IS NULL GROUP BY q.id;`
- ⚠ 📋 Phase 4c — **Quest-table completeness is unmeasured: DB has 681 quests; the wiki's `Quests` hub reports 869 (as of Update 80.0.1, 2026-07).** Not directly comparable — the wiki counts a quest once for heroic and once for epic, excludes challenges/Lamannia, and the DB mixes real quests with loot-source rows (`Altar of Fecundity`) — but the gap is large enough that recent content is likely missing (two known: `Threats Old and New`, `Den of Vipers`). The wiki's `Level_N_quests` pages (N = 1–40, linked from `/page/Quests`) enable a precise per-level audit; also check `Retired quests` before flagging a DB row with no wiki page as junk. See "Finding quests and raids" in [ddowiki-api.md](../ddowiki-api.md).
- ✅ **Bogus quest `The Chronoscope reward items` (was 8 loot rows) — fixed 2026-07-25.** Root cause: the `Raid_loot` subcategory `The Chronoscope reward items` ends in `" reward items"` while the scraper stripped only that parent's `" loot"` suffix, so the raw title was auto-created as a quest. Scraper now strips against every known suffix regardless of parent (`_quest_name_from_subcategory`, covered by `test_collect_quest_loot_strips_any_known_suffix_regardless_of_parent`); the DB rows were merged into `The Chronoscope` (all 8 were duplicates of existing rows) and the bogus quest deleted.
- 🐛 📋 Phase 4c — **`quest_loot.loot_type` is populated by an offline backfill, not the wiki.** 651 rows are tagged `raid` (609 distinct items); the remaining 4,306 rows are NULL rather than `chest`/`reward`, because `collect_quest_loot` can't run while ddowiki sits behind the AWS WAF challenge (see [ddowiki-api.md](../ddowiki-api.md)). The scraper already emits the right `loot_type` per category, so a single successful items scrape fills all three values and makes `scripts/src/ddo_data/game_data/raid_quests.py` deletable. Until then the chest/reward distinction is unavailable. Reproduce: `SELECT COALESCE(loot_type,'(null)'), COUNT(*) FROM quest_loot GROUP BY loot_type;`
